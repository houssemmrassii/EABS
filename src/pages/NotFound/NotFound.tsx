/* type Props = {}; */

const NotFound = (/* props: Props */) => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Page Not Found
    </div>
  );
};

export default NotFound;
